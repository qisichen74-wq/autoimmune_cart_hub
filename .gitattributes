# ⚠️ 全部文件按原字节存储，禁止 git 做 CRLF/LF 自动转换。
# 理由：本项目的验收标准是"同一份输入必须产出逐字节相同的输出"。
# 在 Windows 上 git 默认会把提交进仓库的文本转成 LF、签出时转成 CRLF；
# 一旦导出器在这台机器上生成 CRLF 而仓库里存的是 LF，
# 每次发布都会产生一份"全文件都变了"的假 diff，
# 真正的内容变化就此被埋在噪声里 —— 而且这在本地看不出来。
* -text
